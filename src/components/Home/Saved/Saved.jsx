import React from 'react';
import Header from '../../Shared/Header/Header';

const Saved = () => {
    return (
        <div>
            <Header/>
            Saved
        </div>
    );
};

export default Saved;