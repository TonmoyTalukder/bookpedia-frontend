import React from 'react';
import Header from '../../Shared/Header/Header';

const Library = () => {
    return (
        <div>
            <Header/>
            Library
        </div>
    );
};

export default Library;